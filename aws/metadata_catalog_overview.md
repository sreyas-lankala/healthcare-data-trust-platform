# Metadata Catalog Overview

The Healthcare Data Trust Platform uses the AWS Glue Data Catalog
to manage metadata for datasets stored in the S3 data lake.

The catalog stores information such as table schemas,
column types, and dataset locations.

This enables query engines like Amazon Athena to interpret
raw datasets stored in the data lake.